# Backend configuration (DO NOT put secrets in source control)

# =========================
# Database
# =========================
# Option A (recommended): full SQLAlchemy URL
# Example:
# DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5001/mydb
DATABASE_URL=

# Option B: DB container env vars (used if DATABASE_URL is empty)
# Provided by the PostgreSQL container:
POSTGRES_URL=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=

# =========================
# Auth / JWT
# =========================
# REQUIRED: set a strong secret in real deployments.
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=4320

# =========================
# Media / Uploads
# =========================
# Directory where uploaded mp3 files are stored on the backend filesystem.
MEDIA_ROOT=media
# Max upload size in bytes (default 50MB)
MAX_UPLOAD_BYTES=52428800

# =========================
# CORS
# =========================
# The backend allows http://localhost:3000 by default.
# Add additional allowed origins (comma-separated) for preview/deploy.
# Example:
# CORS_ALLOW_ORIGINS=https://your-frontend-preview-host:3000
CORS_ALLOW_ORIGINS=
